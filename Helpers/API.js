const Axios = require("axios");

module.exports = {
    get(...args) {
        const [data, headers] = args;
    },
    post(...args) {
        const [data, headers] = args;
    },
    put(...args) {
        const [data, headers] = args;
    },
    patch(...args) {
        const [data, headers] = args;
    },
    del(...args) {
        const [data, headers] = args;
    }
}